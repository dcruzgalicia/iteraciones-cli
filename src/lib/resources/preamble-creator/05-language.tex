% Idioma con babel (configurable por el lang de la configuración; el valor lo
% expone el CLI como metadata babel-lang en cada conversión markdown → latex)
$if(babel-lang)$
\usepackage[$babel-lang$]{babel}
$else$
\usepackage[spanish,es-noshorthands,es-noindentfirst]{babel}
$endif$
